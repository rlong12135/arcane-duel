# CI Helpers

Use this folder for lightweight local CI-style helpers:
- validation wrappers
- report generators
- changelog helpers
- backup/export metadata scripts

Prefer scripts that can run both locally and in containers.
