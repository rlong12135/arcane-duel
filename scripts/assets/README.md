# Asset Scripts

Put repeatable asset processing helpers here:
- icon generation
- batch renaming
- atlas prep
- format conversion

Keep scripts deterministic so they can be rerun on Linux or Windows.
